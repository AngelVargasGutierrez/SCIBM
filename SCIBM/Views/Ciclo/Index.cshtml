@model IEnumerable<SCIBM.Models.CicloAcademico>

@{
    ViewBag.Title = "Ciclos Académicos";
    ViewBag.Subtitle = "Gestione sus ciclos y acceda a los cursos de cada uno";
}

<style>
    /* COURSE GRID */
    .courses-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: 24px;
    }

    .course-card {
        background: var(--glass-bg);
        border: 1px solid var(--glass-border);
        border-radius: 16px;
        padding: 24px;
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        transition: all 0.3s ease;
        display: flex;
        flex-direction: column;
        gap: 16px;
        position: relative;
        overflow: hidden;
    }
    .course-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 4px;
        background: linear-gradient(90deg, var(--accent-color), var(--primary-glow));
        opacity: 0.8;
    }
    .course-card:hover {
        transform: translateY(-5px);
        border-color: rgba(100, 223, 223, 0.3);
        box-shadow: 0 12px 40px 0 rgba(0, 180, 216, 0.15);
    }

    .course-info h3 {
        font-size: 24px;
        font-weight: 700;
        margin-bottom: 4px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        color: var(--text-color);
    }

    .course-stats {
        display: flex;
        align-items: center;
        gap: 20px;
        padding: 12px 0;
        border-top: 1px solid var(--glass-border);
        border-bottom: 1px solid var(--glass-border);
    }
    .stat-item {
        display: flex;
        flex-direction: column;
        gap: 4px;
    }
    .stat-val {
        font-size: 18px;
        font-weight: 700;
        color: var(--primary-glow);
    }
    .stat-lbl {
        font-size: 11px;
        color: var(--text-muted);
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    /* ACTION BAR & BREADCRUMB */
    .action-bar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
    }
    .breadcrumb {
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 16px;
        font-weight: 500;
        color: var(--text-muted);
    }
    .breadcrumb-current {
        color: var(--primary-glow);
    }
    
    /* NO DATA EMPTY STATE */
    .empty-state {
        text-align: center;
        padding: 60px 40px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 20px;
    }
    .empty-icon {
        font-size: 64px;
        color: var(--text-muted);
        opacity: 0.4;
        animation: float 3s ease-in-out infinite;
    }
    
    @@keyframes float {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
    }
    
    .edit-btn {
        color: var(--text-color);
        background: none;
        border: none;
        font-size: 16px;
        cursor: pointer;
        opacity: 0.6;
        transition: all 0.2s ease;
        padding: 4px;
    }
    .edit-btn:hover {
        color: var(--accent-color);
        opacity: 1;
        transform: scale(1.1);
    }
    .delete-btn {
        color: var(--text-color);
        background: none;
        border: none;
        font-size: 16px;
        cursor: pointer;
        opacity: 0.6;
        transition: all 0.2s ease;
        padding: 4px;
    }
    .delete-btn:hover {
        color: #ff5252;
        opacity: 1;
        transform: scale(1.1);
    }
</style>

<!-- ACTION BAR -->
<div class="action-bar">
    <div class="breadcrumb">
        <span class="breadcrumb-current"><i class="fa-solid fa-calendar-days" style="margin-right: 6px;"></i> Ciclos Académicos</span>
    </div>
    <button class="btn btn-primary" onclick="openModal()">
        <i class="fa-solid fa-plus"></i> Nuevo Ciclo
    </button>
</div>

<!-- CICLOS LISTING -->
@if (Model == null || !Model.Any())
{
    <div class="glass-card empty-state">
        <i class="fa-solid fa-calendar-days empty-icon"></i>
        <h2>No hay ciclos académicos</h2>
        <p style="color: var(--text-muted); max-width: 400px; font-size: 15px;">
            Aún no has creado ningún ciclo (Ej. 2026-I). Crea uno para empezar a organizar tus cursos.
        </p>
        <button class="btn btn-primary" onclick="openModal()">
            Crear mi primer ciclo
        </button>
    </div>
}
else
{
    <div class="courses-grid">
        @foreach (var c in Model)
        {
            <div class="course-card">
                <div class="course-info" style="display: flex; align-items: flex-start; justify-content: space-between;">
                    <h3 title="@c.Nombre" style="margin-bottom: 0;">@c.Nombre</h3>
                    <div style="display: flex; gap: 8px;">
                        <button class="edit-btn" onclick="openEditModal('@c.Id', '@c.Nombre')" title="Renombrar">
                            <i class="fa-solid fa-pencil"></i>
                        </button>
                        <button class="delete-btn" onclick="openDeleteModal('@c.Id', '@c.Nombre')" title="Eliminar">
                            <i class="fa-solid fa-trash-can"></i>
                        </button>
                    </div>
                </div>
                
                <div class="course-stats">
                    <div class="stat-item">
                        <span class="stat-val">@c.Cursos.Count</span>
                        <span class="stat-lbl">Cursos</span>
                    </div>
                    @if (c.DriveFolderId != null)
                    {
                        <div class="stat-item" style="margin-left: auto;">
                            <span class="stat-val" style="color: var(--primary-glow);"><i class="fa-brands fa-google-drive"></i></span>
                            <span class="stat-lbl">Sincronizado</span>
                        </div>
                    }
                </div>

                <a href="@Url.Action("Index", "Curso", new { cicloId = c.Id })" class="btn btn-secondary" style="width: 100%;">
                    Ver Cursos <i class="fa-solid fa-arrow-right" style="margin-left: 6px; font-size: 12px;"></i>
                </a>
            </div>
        }
    </div>
}

<!-- MODAL ADD CICLO -->
<div class="modal-overlay" id="add-ciclo-modal">
    <div class="modal-box">
        <div class="modal-header">
            <h3>Nuevo Ciclo Académico</h3>
            <button class="modal-close" onclick="closeModal()"><i class="fa-solid fa-xmark"></i></button>
        </div>
        
        <form id="create-ciclo-form" onsubmit="submitForm(event)">
            <div class="form-group">
                <label for="nombre">Nombre del Ciclo</label>
                <input type="text" id="nombre" name="nombre" class="form-input" placeholder="Ej: 2026-I" required />
            </div>

            <div id="modal-error" style="color: #ff5252; font-size: 13px; margin-bottom: 15px; display: none;"></div>

            <div style="display: flex; justify-content: flex-end; gap: 12px; margin-top: 25px;">
                <button type="button" class="btn btn-secondary" onclick="closeModal()">Cancelar</button>
                <button type="submit" class="btn btn-primary" id="submit-btn">
                    <span>Crear Ciclo</span>
                </button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL EDIT CICLO -->
<div class="modal-overlay" id="edit-ciclo-modal">
    <div class="modal-box">
        <div class="modal-header">
            <h3>Renombrar Ciclo</h3>
            <button class="modal-close" onclick="closeEditModal()"><i class="fa-solid fa-xmark"></i></button>
        </div>
        
        <form id="edit-ciclo-form" onsubmit="submitEditForm(event)">
            <input type="hidden" id="edit-id" />
            <div class="form-group">
                <label for="edit-nombre">Nuevo Nombre</label>
                <input type="text" id="edit-nombre" class="form-input" required />
            </div>

            <div id="edit-modal-error" style="color: #ff5252; font-size: 13px; margin-bottom: 15px; display: none;"></div>

            <div style="display: flex; justify-content: flex-end; gap: 12px; margin-top: 25px;">
                <button type="button" class="btn btn-secondary" onclick="closeEditModal()">Cancelar</button>
                <button type="submit" class="btn btn-primary" id="edit-submit-btn">
                    <span>Guardar Cambios</span>
                </button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL DELETE CICLO -->
<div class="modal-overlay" id="delete-ciclo-modal">
    <div class="modal-box">
        <div class="modal-header">
            <h3>Eliminar Ciclo</h3>
            <button class="modal-close" onclick="closeDeleteModal()"><i class="fa-solid fa-xmark"></i></button>
        </div>
        <p style="margin-bottom: 20px; font-size: 15px; color: var(--text-muted);">
            ¿Estás seguro que deseas eliminar el ciclo <strong id="delete-ciclo-nombre" style="color: white;"></strong>? <br><br>
            <span style="color: #ff5252;"><i class="fa-solid fa-triangle-exclamation"></i> Advertencia:</span> Esto eliminará permanentemente todos los cursos, secciones y exámenes asociados.
        </p>
        <form id="delete-ciclo-form" onsubmit="submitDeleteForm(event)">
            <input type="hidden" id="delete-id" />
            <div id="delete-modal-error" style="color: #ff5252; font-size: 13px; margin-bottom: 15px; display: none;"></div>
            <div style="display: flex; justify-content: flex-end; gap: 12px; margin-top: 10px;">
                <button type="button" class="btn btn-secondary" onclick="closeDeleteModal()">Cancelar</button>
                <button type="submit" class="btn btn-primary" id="delete-submit-btn" style="background: #ff5252; box-shadow: 0 4px 15px 0 rgba(255, 82, 82, 0.4); border: none;">
                    <span>Sí, Eliminar</span>
                </button>
            </div>
        </form>
    </div>
</div>

@section scripts {
    <script>
        function openModal() {
            document.getElementById("add-ciclo-modal").classList.add("active");
            document.getElementById("nombre").focus();
        }

        function closeModal() {
            document.getElementById("add-ciclo-modal").classList.remove("active");
            document.getElementById("create-ciclo-form").reset();
            document.getElementById("modal-error").style.display = "none";
        }

        function openEditModal(id, nombre) {
            document.getElementById("edit-id").value = id;
            document.getElementById("edit-nombre").value = nombre;
            document.getElementById("edit-ciclo-modal").classList.add("active");
            document.getElementById("edit-nombre").focus();
        }

        function closeEditModal() {
            document.getElementById("edit-ciclo-modal").classList.remove("active");
            document.getElementById("edit-ciclo-form").reset();
            document.getElementById("edit-modal-error").style.display = "none";
        }

        function submitEditForm(e) {
            e.preventDefault();
            
            var submitBtn = document.getElementById("edit-submit-btn");
            var originalHtml = submitBtn.innerHTML;
            
            submitBtn.disabled = true;
            submitBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Guardando...';

            var id = document.getElementById("edit-id").value;
            var newName = document.getElementById("edit-nombre").value;

            fetch('@Url.Action("Rename", "Ciclo")', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded',
                },
                body: 'id=' + encodeURIComponent(id) + '&newName=' + encodeURIComponent(newName)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    location.reload();
                } else {
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = originalHtml;
                    var errDiv = document.getElementById("edit-modal-error");
                    errDiv.innerText = data.message || "Ocurrió un error al renombrar.";
                    errDiv.style.display = "block";
                }
            })
            .catch(error => {
                submitBtn.disabled = false;
                submitBtn.innerHTML = originalHtml;
                var errDiv = document.getElementById("edit-modal-error");
                errDiv.innerText = "Error de conexión con el servidor.";
                errDiv.style.display = "block";
            });
        }

        function openDeleteModal(id, nombre) {
            document.getElementById("delete-id").value = id;
            document.getElementById("delete-ciclo-nombre").innerText = nombre;
            document.getElementById("delete-ciclo-modal").classList.add("active");
        }

        function closeDeleteModal() {
            document.getElementById("delete-ciclo-modal").classList.remove("active");
            document.getElementById("delete-modal-error").style.display = "none";
        }

        function submitDeleteForm(e) {
            e.preventDefault();
            
            var submitBtn = document.getElementById("delete-submit-btn");
            var originalHtml = submitBtn.innerHTML;
            
            submitBtn.disabled = true;
            submitBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Eliminando...';

            var id = document.getElementById("delete-id").value;

            fetch('@Url.Action("Delete", "Ciclo")', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded',
                },
                body: 'id=' + encodeURIComponent(id)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    location.reload();
                } else {
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = originalHtml;
                    var errDiv = document.getElementById("delete-modal-error");
                    errDiv.innerText = data.message || "Ocurrió un error al eliminar.";
                    errDiv.style.display = "block";
                }
            })
            .catch(error => {
                submitBtn.disabled = false;
                submitBtn.innerHTML = originalHtml;
                var errDiv = document.getElementById("delete-modal-error");
                errDiv.innerText = "Error de conexión con el servidor.";
                errDiv.style.display = "block";
            });
        }

        function submitForm(e) {
            e.preventDefault();
            
            var submitBtn = document.getElementById("submit-btn");
            var originalHtml = submitBtn.innerHTML;
            
            submitBtn.disabled = true;
            submitBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Creando...';

            var nombre = document.getElementById("nombre").value;

            fetch('@Url.Action("Create", "Ciclo")', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded',
                },
                body: 'nombre=' + encodeURIComponent(nombre)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    location.reload();
                } else {
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = originalHtml;
                    var errDiv = document.getElementById("modal-error");
                    errDiv.innerText = data.message || "Ocurrió un error al crear el ciclo.";
                    errDiv.style.display = "block";
                }
            })
            .catch(error => {
                submitBtn.disabled = false;
                submitBtn.innerHTML = originalHtml;
                var errDiv = document.getElementById("modal-error");
                errDiv.innerText = "Error de conexión con el servidor.";
                errDiv.style.display = "block";
            });
        }
    </script>
}
